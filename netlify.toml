[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions."send-email"]
  timeout_seconds = 60

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
